# IMP-014: Deduplicate Fmi2Error/Fmi2Fatal Cleanup Documentation

## Status

Proposed

## Layer

03-implementation

## Theme

The same note about post-error FMU cleanup appears in two files. Consolidate to one and cross-link.

## Evidence

Two occurrences:

1. `tests/README.md`, lines 91-93: "After an FMU reaches `fmi2Error` or `fmi2Fatal`, cleanup frees the instance without calling `fmi2Terminate` so logs keep the original step failure as the root cause."
2. `product-breakdown/03-implementation/modules/handler.md`, line 29: "After fmi2Error or fmi2Fatal, cleanup frees the instance without calling fmi2Terminate (so logs keep the root cause)."

## Current Pain Or Risk

- Minor duplication. If the cleanup behavior changes, both locations need updating.

## Proposed Improvement

1. Keep the canonical note in `tests/README.md` (it documents test behavior rationale — why tests expect specific log output after FMU errors).
2. In `product-breakdown/03-implementation/modules/handler.md`, replace the note with a cross-link: "See `tests/README.md` for post-error cleanup behavior."

## Expected Benefit

- Single source for FMU lifecycle edge-case documentation.

## Risk And Blast Radius

- **Blast radius**: 2 files, one-line change in handler.md.
- **Risk**: Low.

## Suggested Priority

Medium — quick fix, improves clarity.

## Task Contract Seed

The smallest scoped task:

1. Open `product-breakdown/03-implementation/modules/handler.md`.
2. Replace line 29 with: "See `tests/README.md` for post-error cleanup behavior."
3. Ensure `tests/README.md` still has the canonical note.

## Out Of Scope

- Do not modify the cleanup behavior in C++ code.
- Do not change tests/README.md.

## Traceability

- Intent: Single source of truth for FMU lifecycle edge-case behavior.
- Product: N/A
- Architecture: N/A
- Implementation: handler.md
- Verification: tests/README.md

## Notes

- The handler module documentation should focus on handler responsibilities, not test-observable behavior. Linking to tests/README.md is appropriate.