# IMP-029: Implement SQLite WAL Local Database Sink

## Status

Proposed

## Layer

03-implementation (recording sinks), 01-product (result artifact model)

## Theme

Add a SQLite WAL local database sink so the primary local result artifact can
serve live one-writer, many-reader viewer workflows without repeated CSV scans.

## Evidence

Current support is limited to file-backed sinks:

- `CsvRecorderSink` writes portable row-oriented CSV output
- `DuckDBRecorderSink` writes local database files with per-storage tables
- `OD-002` states that streaming, socket, and network output are unavailable

The current DuckDB backend is useful for post-run analysis, but it is not the
best fit for a separate live writer and viewer process sharing one mutable
database file. SQLite WAL better matches the one-local-writer, many-local-reader
viewer workflow.

## Proposed Improvement

1. Add a SQLite WAL local database sink for live local viewer workflows.
2. Keep DuckDB as the current analytical backend and interop/export path.
3. Define common result metadata across local database sinks: run id, model,
   storage, variable, timestamp, simulation time, and source identity.
## Tradeoffs

- SQLite WAL is simple and supports concurrent local readers, but it will lose
  to columnar engines for large historical scans.
- DuckDB remains strong for analytical scans and table export, but local mutable
  file concurrency is not the right shape for a separate live viewer process.
- CSV remains valuable as the lowest-friction export even when database sinks
  are enabled.

## Acceptance Criteria

- Product docs describe the primary local database artifact without overstating
  current implementation.
- Local database sink selection is explicit in configuration.
- Existing CSV and DuckDB behavior remains compatible by default.

## Related Artifacts

- `docs/usage.md`
- `product-breakdown/01-product/decisions/PD-001.md`
- `product-breakdown/01-product/decisions/PD-003.md`
- `product-breakdown/05-operation/decisions/OD-002.md`
