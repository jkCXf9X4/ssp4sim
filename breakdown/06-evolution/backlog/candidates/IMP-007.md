# IMP-007: Rename jacobi_serial to Gauss-Seidel + hybrid executor design sketch

## Lifecycle Stage
Candidate

## Status
Proposed

## Layer
03-implementation

## Theme

Rename the `jacobi_serial` executor variant to `Gauss-Seidel` to reflect the actual
algorithm, and sketch a hybrid executor design that builds on the renamed family.

## Evidence

- `lib/include/simulation/executor_builder.cpp` — registers the `jacobi_serial` variant
- `lib/include/simulation/executor/jacobi/jacobi_serial.cpp` — the sequential Jacobi implementation

## Current Pain Or Risk

The name `jacobi_serial` is misleading: the sequential variant is algorithmically a
Gauss-Seidel iteration. The rename is still open — referenced as complementary by
`candidates/IMP-025.md` (line 223) and `done/IMP-028.md` (line 219).

## Proposed Improvement

1. Rename `jacobi_serial` → `gauss_seidel` in `executor_builder.cpp`, module docs, and `docs/configuration.md`.
2. Add a hybrid executor design sketch (building on IMP-021/IMP-025) that uses the renamed family.

## Risk / Blast Radius

Rename: executor_builder.cpp, docs/configuration.md, module docs (moderate).
Design sketch: documentation only (low).

## Suggested Priority: Medium

## Sources

- `lib/include/simulation/executor_builder.cpp`
- `lib/include/simulation/executor/jacobi/jacobi_serial.cpp`