# IMP-045: Investigate signal_sine_gain_add tree_output/graph_output discrepancy

## Lifecycle Stage
Candidate

## Status
Proposed

## Evidence

Root-level `tmp.md` (deleted) contained an open investigation prompt: simulating
`resources/signal_sine_gain_add.json` produced a `wd/signal_sine_gain_add/baseline/tree_output.txt`
and `wd/signal_sine_gain_add/baseline/graph_output.dot` that "seems a bit off".
`resources/signal_sine_gain_add.json` exists, but `wd/signal_sine_gain_add/` does not
(wd/ contains only embrace*, loop_aware_nested dirs) — the referenced baseline outputs
never existed or were removed. The prompt was unowned and referenced nowhere in docs/ or AGENTS.md.

Sources: git history of `tmp.md` (untracked, gitignored; content preserved in this entry).

## Current Pain / Risk

- The tree_output/graph_output discrepancy for `signal_sine_gain_add` was never investigated
  or resolved; if it reflects a real bug in tree/graph output generation, it is still latent.
- The investigation prompt was unowned, so the work item had no owner or tracking.

## Proposed Improvement

1. Re-run the simulation of `resources/signal_sine_gain_add.json` and compare
   `tree_output.txt` / `graph_output.dot` against expected output.
2. Determine whether the discrepancy is a bug in output generation or stale/expected behavior.
3. If a bug, file a fix; otherwise close this item with a note.